#include <stdio.h>
#include <stdlib.h>

struct ListNode { 
    int value ;
    struct ListNode *next ;
} ;

struct ListNode* removeNthFromEnd(struct ListNode* head, int n) ; // declaration
struct ListNode* createLinkedList(int numOfNodes) ;

int main () {
    int numOfNodes ;
    struct ListNode *n1 ;
    struct ListNode *head ;

    printf("Enter the number of nodes in the Linked List: ") ;
    scanf("%d", numOfNodes) ;
}
struct ListNode* createLinkedList(int numOfNodes) 
{   
    while (numOfNodes > 0) {
        printf("Enter the value of the %d node: ") ;
        scanf("%d", )
    }
}
struct ListNode* removeNthFromEnd(struct ListNode* head, int n)
{
    
}