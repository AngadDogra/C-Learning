// ANGAD DOGRA AIML-H 

// THIS IS A CODE FOR THE HEAP SORT ALGORITHM 

// DEFINITIONS OF IMPORTANT PROPERTIES
        // HEAP - A heap can be viewed as a nearly complete binary tree
        //        it is an array obejct which arranges it in an order of 
        //        parent-child relations
        //
        // MAX-HEAP - A heap which maintains the following property 
        //              A[PARENT(i)] >= A[i]  ---> parent is bigger than the child
        //
        // MIN-HEAP - A heap which maintains the following property
        //              A[PARENT(i)] <= A[i]  ---> parent is smaller than the child               

// ALGORITHMS IMPLEMENTED - 
        // PARENT(i) = returns i/2 node of the heap
        // LEFT(i) = returns 2i node of the heap
        // RIGHT(i) =  returns 2i+1 node of the heap 
        // HEAPIFY(A,i) = sorts the  

#include <stdio.h>

int main() {


}