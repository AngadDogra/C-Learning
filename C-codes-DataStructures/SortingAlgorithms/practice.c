#include <stdio.h>

int main(){
	//program to check compilation in terminal
	int num ;
	printf("enter the value of the number: ");
	scanf("%d",&num);
	printf("\n The value of the number is: %d",num);

	return 0;
}
